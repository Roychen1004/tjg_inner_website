# 開發指南

架構、規範、UI 原則各取必要的部分。長版在 `../封存/`。

## 架構

```
nginx (:30080)  靜態檔 + 反向代理 + /protected-media/（internal，附件下載）
  └ api        Django 5 + DRF + gunicorn（3 workers）
  └ db         PostgreSQL 16
前端建置期打包成靜態檔——正式環境沒有任何 Node 程序
```

Django apps：`core`（帳號/附件/通知）、`masters`（客戶/廠商/流程目錄/階段模板）、
`projects`、`tracking`、`billing`（應收）、`payables`（應付+現金流）、`analytics`（儀表板）。

資料模型的骨架（2026-08-14 流程制，其餘都是配角）：

```
FlowStage(5) ─ FlowItem(19)     流程目錄（Admin 維護；順序對使用者唯讀）
Project ─┬─ FlowUnit            建案勾一項生一張：負責人/預計起訖/數量。
         │     │                （D43：detail 已併入 description、UI 不再用——
         │     │                卡片只有一個「工作內容」文字區塊）
         │     │                狀態機 todo→doing→done(+na)；負責人可動自己的單元。
         │     │                ＋工作內容/產出物/完成條件（D40：FlowUnit.create_for
         │     │                從目錄抄預設值進單元，之後每案可改；目錄只影響新案；
         │     │                D42：UI 只呈現 description，另兩欄留在 DB 與 API 不再顯示；
         │     │                D41：負責人預設＝project.created_by）
         │     └ FlowTask       工作項目（內容物清單）：名稱/數量/單位/狀態
         │           │          ＋statuses（D45）＝**每個項目自己的**工段清單（JSON，
         │           │          從空開始由使用者加；建議字在 /options task_status_suggestions；
         │           │          FlowUnit.task_statuses 欄位退役留在 DB）
         │           └ FlowTaskAssignment  工段分配（D41）：頭尾以外的狀態＝工段，
         │                      status×assignee×qty；員工回報 qty_done、做完通知單元負責人。
         │                      FlowTask.progress_pct＝各工段完成度平均（需 prefetch assignments）
         ├─ BillingMilestone    應收款：未到→可請款→已請款→已收款（一列＝一期）
         │     └ trigger_unit → FlowUnit 完成時自動 PENDING→CLAIMABLE＋通知；
         │       一單元只能掛一期（write serializer 擋）；D46 反向同步：
         │       重啟或取消連結時 CLAIMABLE 退回 PENDING（trigger_service.
         │       on_trigger_reopened / revert_claimable），已請款只發警示不動；
         │       accountant（D45）＝負責收款的會計師：觸發時必通知、
         │       出現在他 MyWork 的待收款（GET /billing-milestones?accountant=me）
         │       員工視圖：GET /staff-workload?month=YYYY-MM（D46，無金額）
         ├─ TrackingUnit        構件批次（鋼構七站）。過站 → sync_batch_rollup()
         │                      回寫 4.1/4.2/4.3/4.5 的 FlowUnit（批次是唯一事實來源）
         │                      UI 只在專案明細的「構件批次」區塊（看板沒有批次視圖）
         ├─ Subcontract ─ Payable   應付：待計價→已核可→已付款；可掛 flow_unit
         └─ ChangeOrder         核准後未請款的期數自動重算金額

Project.lifecycle：active/lost/paused/closed；is_closed 是影子欄位（save() 同步）。
amount_base = 有效合約額，沒有就 estimate_amount（估價案的預測用「預估級」）。

DELETE /projects/{id}：限經理（delete_project）。已有請款/收款紀錄或掛著
分包合約/應付款的擋下（錢的歷程不可竄改）；其餘 CASCADE 連附件檔案一併清掉。

權限（D40，矩陣在 main/utils/permissions.py）：經理＋superuser 改一切；
會計師只改金流（edit_milestone/edit_payable/pay_payable…，approve_payable 仍限經理）；
員工全唯讀（金額拿不到），僅 can_operate 自己被指派的單元；
設定頁人人可看（NAV settings=None），寫入靠 manage_masters 擋。
帳號名冊 15 人與密碼在 docs/帳號密碼.md（老闆要求明文）。

遺留欄位：main_template/main_stage 還在 DB（NOT NULL；migrations 只加不改，
建案時仍寫入預設值），但 API 不再輸出、advance-stage 端點已移除（D39）——
主線由前端從 flow_units 自動判定，結案走「編輯專案」改 lifecycle。

甘特：專案卡片迷你甘特（五階段 bar＋日期網格）、看板日曆甘特（D40：滾輪
縮放 7 天～1 年、拖曳平移，表頭密度隨 px/天 自動調）、
GET /projects/{id}/gantt-xlsx?variant=progress|plan 下載 Excel
（openpyxl，`projects/services/gantt_export.py`；progress＝內部進度、plan＝業主工期規劃）。
```

附件檔案存哪：named volume `media` → 容器內 `/app/media`（MEDIA_ROOT），
路徑 `attachments/YYYY/MM/<uuid>.<副檔名>`——刻意不用原始檔名（亂碼、
覆蓋、路徑注入），原始檔名存欄位、下載時還原。下載走 Django 驗權限後
X-Accel-Redirect 給 nginx 的 internal `/protected-media/`。

## 鐵律（違反過、修過、不要再犯）

1. **金額用 Decimal**；「沒填」與「填 0」是兩件事——判斷用 `"field" not in attrs`，不用 `if not value`
2. **權限檢查在業務規則之前**——先 403 再 400，否則等於告訴無權限的人「補個欄位就能過」
3. **DB CHECK 是最後防線不是錯誤訊息**——view 層先驗，給人看得懂的中文
4. 列表一律 `select_related/prefetch_related`；兩個 Sum 走不同關聯要用 Subquery，
   不然 JOIN 列數相乘、金額安靜地變大
5. 錢的狀態機：往前一次一步、往回必填原因、寫入不可竄改的 Log 表
6. 附件：magic bytes 驗檔、照片先 `exif_transpose` 再剝 EXIF、
   下載走 X-Accel-Redirect（nginx 要自己再設一次 nosniff——它不轉發上游自訂標頭）
7. 金額型附件（合約/發票）跟著 `view_money` 權限走，清單也不出現
8. 範圍外資料回 404 不回 403
9. 錯誤訊息說「怎麼辦」，不只說「不行」

## API 慣例

- Session + CSRF（同網域），路徑 `/api/v0.1/...` 結尾不加斜線
- 狀態轉換用具名 action（`POST .../transition`），不用 PATCH 欄位
- `next_states` 由後端算好回給前端——狀態機只有一份
- 型別鏈：`manage.py spectacular --file openapi.yaml` → `npm run types` → `schema.d.ts`。
  改了 API 一定重跑，不手抄型別

## UI 原則

- 六個分頁，一個畫面回答一個問題；分頁由後端 `visible_nav` 決定
- 元素的存在要通過「砍掉它，有人會做不了事嗎」
- 顏色永遠伴隨文字標籤；金額欄位無權限時是 null（不是 0）
- 手機優先：路由 lazy 載入、首屏 gzip 預算 200KB（現況約 68KB + 頁面塊）
- 共用格式函式放 `web/src/lib/`，不從頁面 import——會把整頁拉進別人的 chunk

## 驗證

```bash
.venv/bin/ruff check main/ shell/          # 後端 lint
docker run --rm -v $PWD/web:/w -w /w node:22-alpine npx tsc -b   # 前端型別
python3 shell/verify.py                    # 全流程整合驗收（系統要先起來；跑完 seed_demo --clear 還原）
```

⚠️ api 映像**沒有掛原始碼**——改了後端要 `docker compose build api && docker compose up -d`
才會生效（AUTO_MIGRATE 會在啟動時套 migrations）。前端同理 `build web`。
migrations 一律**只加不改**（系統已有資料）。
